name: categorical
type: string
template: |
  Given the concept of {concept} {description_clause},
  provide a list of common {domain}s for this concept.
  Return only JSON with key:
    - {domain} (string)