name: measurement
type: float
template: |
  Given the {concept} (which is a {description}),
  provide the average {domain} as in {dimension} measured in {measurement}.
  Return only JSON with key:
    - {dimension} (float)