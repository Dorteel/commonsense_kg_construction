Given the concept of {emotion_label} (which is defined as {def_APA}),
provide an estimation of the average {dim_label} (which is {dim_definition})
in the range of {dim_range}.
Return only JSON with key:
  - {dim_label} (float)
