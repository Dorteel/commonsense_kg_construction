name: measurement_range
type: float
template: |
  Given the concept of {concept} {description_clause},
  provide the typical range {domain}{dimension_clause} measured in {measurement}.
  Return only JSON with keys:
    - min_{domain} (float)
    - max_{domain} (float)